# EDA-отчёт

Исходный файл: `example.csv`

Строк: **20**, столбцов: **10**

## Параметры генерации отчёта

- Максимум гистограмм: **6**
- Top-K категорий: **5**
- Порог пропусков: **10%**
- Порог высокой кардинальности: **50** уникальных значений
- Порог доли нулей: **50%**

## Качество данных (эвристики)

- Оценка качества: **0.65**
- Макс. доля пропусков по колонке: **15.00%**
- Слишком мало строк: **True**
- Слишком много колонок: **False**
- Слишком много пропусков: **False**
- Константные колонки: **False**
- Высокая кардинальность категорий (порог >50): **False**
- Подозрительные дубликаты ID: **False**
- Много нулевых значений (порог >50%): **False**

## Колонки

См. файл `summary.csv`.

## Пропуски

См. файлы `missing.csv` и `missing_matrix.png`.

### Проблемные колонки (доля пропусков >= 10%)

- **email**: 15.0% (3 пропусков)

## Корреляция числовых признаков

См. `correlation.csv` и `correlation_heatmap.png`.

## Категориальные признаки

Показаны top-5 значений для каждого признака.

См. файлы в папке `top_categories/`.

## Гистограммы числовых колонок

См. файлы `hist_*.png`.
